<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="MaterialShapeAppearance">
        <attr name="backgroundColor" format="color" />

        <!-- Corner size to be used in the ShapeAppearance. All corners default to this value -->
        <attr name="allCornersRadius" format="dimension|fraction" />
        <!-- Top left corner size to be used in the ShapeAppearance. -->
        <attr name="cornerRadiusTopLeft" format="dimension|fraction" />
        <!-- Top right corner size to be used in the ShapeAppearance. -->
        <attr name="cornerRadiusTopRight" format="dimension|fraction" />
        <!-- Bottom right corner size to be used in the ShapeAppearance. -->
        <attr name="cornerRadiusBottomRight" format="dimension|fraction" />
        <!-- Bottom left corner size to be used in the ShapeAppearance. -->
        <attr name="cornerRadiusBottomLeft" format="dimension|fraction" />

        <!-- Corner family to be used in the ShapeAppearance. All corners default to this value -->
        <attr name="cornerType" format="enum">
            <enum name="rounded" value="0" />
            <enum name="cut" value="1" />
        </attr>
        <!-- Top left corner family to be used in the ShapeAppearance. -->
        <attr name="cornerTypeTopLeft" format="enum">
            <enum name="rounded" value="0" />
            <enum name="cut" value="1" />
        </attr>
        <!-- Top right corner family to be used in the ShapeAppearance. -->
        <attr name="cornerTypeTopRight" format="enum">
            <enum name="rounded" value="0" />
            <enum name="cut" value="1" />
        </attr>
        <!-- Bottom right corner family to be used in the ShapeAppearance. -->
        <attr name="cornerTypeBottomRight" format="enum">
            <enum name="rounded" value="0" />
            <enum name="cut" value="1" />
        </attr>
        <!-- Bottom left corner family to be used in the ShapeAppearance. -->
        <attr name="cornerTypeBottomLeft" format="enum">
            <enum name="rounded" value="0" />
            <enum name="cut" value="1" />
        </attr>

        <attr name="android:elevation" />
    </declare-styleable>

    <declare-styleable name="PhoneEditText">
        <attr name="android:hint" />
        <attr name="mainPhoneId" format="reference" />
    </declare-styleable>




</resources>